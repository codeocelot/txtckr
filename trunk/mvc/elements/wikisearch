<?php
echo '<div id="wikisearch">';
if (!isset($withinExistingForm) || !$withinExistingForm) {
    echo $form->open();
}
echo $form->input(array('name' => 'wsearch', 'label' => ''))
   . ' ' . $form->input(array('type' => 'submit', 'value' => 'Search'));
if (!isset($withinExistingForm) || !$withinExistingForm) {
   echo $form->close();
}
echo '</div>';